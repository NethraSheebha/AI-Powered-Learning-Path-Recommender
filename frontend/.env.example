# PathMind Frontend Environment Variables
# Copy this to .env.local for local development

# API base URL — points to the FastAPI backend
VITE_API_URL=http://localhost:8000

# Set to 'true' to use mock data without a running backend
# Set to 'false' to use the real backend API
VITE_USE_MOCKS=true
